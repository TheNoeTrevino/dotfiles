keybinding:
  universal:
    prevItem-alt: l
    nextItem-alt: k
    prevBlock-alt: j
    nextBlock-alt: ;
    scrollUpMain-alt1: L
    scrollDownMain-alt1: k

quitOnTopLevelReturn: true

git:
  pagers:
    - pager: delta --dark --paging=never --line-numbers
