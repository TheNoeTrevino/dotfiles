Raspberry Pi/Imager.conf
arandr
balenaEtcher
btop
configstore
dconf
gh
github-copilot
mozilla
pulse 
pnpm
session
dlv
go
crush
yay
zsh-deps
QtProject.conf
dolphinrc
mimeapps.list
raspberry-pi
raspberry-pi.pub
htop
trashrc
goa-1.0
gtk-3.0
ibus
/zen
/discord
/nvm
/spotify
/jellyfin-desktop
/systemd/user
/user-dirs.dirs
/user-dirs.locale
/stripe
/Claude
/FreeCAD
/BambuStudio
/libreoffice/4/user
/OpenSCAD
/chromium/Crash Reports
/zen-backup-20260807/k2alelrl.Default (release)

# ~/.config/git holds the global git config, now that ~/.gitconfig is gone.
# config and ignore are portable, so they are tracked. config-work carries a
# work email and this repo is public, so it stays out. Git ignores a missing
# includeIf path without complaining, so the include in config is harmless on a
# machine that has no config-work.
/git/config-work

# herdr keeps its runtime state next to config.toml. Track only the config.
/herdr/*.log
/herdr/*.sock
/herdr/session.json
/herdr/.plugins.lock
/herdr/plugins/
/herdr/sessions/
# Generated by `herdr plugin install`: absolute paths, a hashed plugin dir and
# an install timestamp. It also caches the manifest, so it goes stale whenever
# the manifest is edited. herdr reads the manifest itself, not this file.
/herdr/plugins.json
/libreoffice/4/cache/sifr_dark/200
/herdr/release-notes.json

# --- Windows machine ---
# opencode installs its plugin deps next to the config. package.json and
# bun.lock are regenerated by `bun install`, so only opencode.jsonc, commands/
# and skills/ are worth tracking. The stray opencode/.gitignore said the same
# thing, but it ignored itself, so it never reached the other machine.
/opencode/node_modules/
/opencode/package.json
/opencode/bun.lock
/opencode/.gitignore

# scoop rewrites last_update on every run, and cagent writes an empty marker
# file on first launch. Both are machine state, not configuration.
/scoop/
/cagent/
